
<div class="container">

<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link" aria-current="page" <%= link_to "All tables", wedding_desks_path(@wedding) %></a>
        </li>
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" <%= link_to "Seating tables", sitting_plan_wedding_path(@wedding) %></a>
        </li>
      </ul>
    </div>
  </div>
</nav>

<h1>My sitting plan</h1>

<div class="card" style="width: 18rem;">
  <div class="card-header">
    Pending Guests
  </div>
  <ul class="list-group list-group-flush">
    <% @pending_guests.each do |pending_guest| %>
    <li class="list-group-item"><%= pending_guest.first_name %> <%= pending_guest.last_name %></li>
    <% end %>
  </ul>
</div>
